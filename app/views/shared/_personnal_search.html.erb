<div class="search-background">
  <div class="container">

      <%= form_tag search_path, method: :get do %>
      <div class="search-grid">
          <div class="search-item">
           <label for="name">Name of the game </label>
            <%= text_field_tag :name,
                               params[:name],
                               class: "form-control",
                               placeholder: "7 wonders" %>
          </div>

          <div class="search-item">
          <label for="min_age">Minimum age </label>
            <%= text_field_tag :min_age,
                               params[:min_age],
                               class: "form-control",
                               placeholder: "10" %>
          </div>

          <div class="search-item", >
          <label for="players_number">Number of players </label>
            <%= text_field_tag :players_number,
                               params[:players_number],
                               class: "form-control",
                               placeholder: "4" %>
          </div>

          <div class="search-item">
          <label for="duration"> Duration <span class="little">(in minutes)</span> </label>
            <%= text_field_tag :duration,
                               params[:duration],
                               class: "form-control",
                               placeholder: "50" %>
          </div>

          <div class="search-btn-2">
            <%= submit_tag "SEARCH", class: "btn-primary" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
